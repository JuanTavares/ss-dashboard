import { GenderToPtPipe } from './gender-to-pt.pipe';

describe('GenderToPtPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderToPtPipe();
    expect(pipe).toBeTruthy();
  });
});
